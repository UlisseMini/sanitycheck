# Database - use docker-compose for local dev, or connect to Railway/other hosted Postgres
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sanitycheck?schema=public"

# Admin dashboard authentication
ADMIN_KEY="changeme-to-something-secure"

# Anthropic API for article analysis (optional - app runs without it, analysis endpoint fails)
ANTHROPIC_API_KEY=""

# Server port (optional, defaults to 3000)
PORT=3000
